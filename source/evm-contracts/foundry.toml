[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "src/script"
ffi = true
gas_reports = ["*"]
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = true
fs_permissions = [{ access = "read-write", path = "./test-results"}]

[profile.release]
libraries = ["src/libraries"]
