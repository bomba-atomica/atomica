# Custom Lighthouse image with curl for healthchecks
# The official sigp/lighthouse image doesn't include curl or wget
FROM sigp/lighthouse:v5.1.0

RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*
