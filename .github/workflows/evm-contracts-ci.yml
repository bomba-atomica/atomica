name: EVM Contracts CI

on:
  push:
    branches: [main, master, debug-e2e]
  pull_request:
    branches: [main, master, debug-e2e]
    types: [opened, synchronize]

jobs:
  # lint-and-build:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Install Foundry
  #       uses: foundry-rs/foundry-toolchain@v1

  #     - name: Run lint
  #       run: forge lint

  #     - name: Run build
  #       run: forge build

  # unit-tests:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Install Foundry
  #       uses: foundry-rs/foundry-toolchain@v1

  #     - name: Run unit tests
  #       run: forge test

  e2e-tests:
    runs-on: ubuntu-latest
    services:
      ethereum-testnet:
        image: ethereum/client-go:latest
        ports:
          - 8545:8545
    defaults:
      run:
        working-directory: source/evm-contracts
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Bun
        run: |
          curl -fsSL https://bun.sh/install | bash
          echo "$HOME/.bun/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: bun install

      - name: Build Ethereum Docker Testnet SDK
        working-directory: source/docker-testnet/ethereum-testnet/typescript-sdk
        run: |
          bun install
          bun run build
          ls -la dist/

      - name: Debug: Check SDK dist folder
        run: |
          ls -la ../../docker-testnet/ethereum-testnet/typescript-sdk/dist/ || echo "Dist folder not found!"

      - name: Run e2e tests
        run: bun run test:docker
        env:
          ETH_RPC_URL: http://localhost:8545