# Zapatos Docker Testnet Configuration
#
# This file configures the Docker images used by the testnet.
# Images are built from local zapatos source via ./build.sh

# Image repository and tag
# The tag is set to the zapatos git commit during build
# Use 'latest' to always use the most recently built image
VALIDATOR_IMAGE_REPO=zapatos-testnet/validator
IMAGE_TAG=latest

# Network configuration
TESTNET_NAME=zapatos-testnet
SUBNET=172.19.0.0/16
GATEWAY=172.19.0.1

# Validator count (4-7 recommended for testing)
NUM_VALIDATORS=4
